export enum EnumMessageType {
    Ping = 1,
    Exchange = 2,
    Error = 3,
    Connect = 4,
    Disconnect = 5,
    Online = 6
}

export enum EnumMessageDataType {
    Text = 1,
    Image = 2,
    Custom = 3
}

export enum EnumChatStatus {
    WAIT = 1,
    READ = 2
}
